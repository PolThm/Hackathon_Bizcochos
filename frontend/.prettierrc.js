module.exports = {
  semi: true, // Enforces the use of semicolons at the end of statements
  trailingComma: 'all', // Adds a trailing comma wherever possible
  singleQuote: true, // Enforces the use of single quotes for strings
  jsxSingleQuote: true, // Enforces the use of single quotes for JSX attributes
  tabWidth: 2, // Specifies the number of spaces per indentation-level
  printWidth: 80, // Specifies the line length that the printer will wrap on
  bracketSpacing: true, // Prints spaces between brackets in object literals
  jsxBracketSameLine: false, // Puts the `>` of a multi-line JSX element at the end of the last line instead of being alone on the next line
  arrowParens: 'always', // Includes parentheses around arrow function arguments
  endOfLine: 'auto', // Maintains end of line consistency
};
